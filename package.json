{
  "name": "knip-platform",
  "version": "0.1.0-alpha-foundation",
  "private": true,
  "description": "KNIP — Knowledge & Narrative Intelligence Platform",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
